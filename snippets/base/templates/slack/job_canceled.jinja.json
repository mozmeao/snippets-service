{
  "attachments": [{
        "pretext": "Update on Job #{{ job.id }}",
        "title": "Job #{{ job.id }} was canceled.",
        "title_link": "{{ job.get_admin_url() }}",
        "text": "Job #{{ job.id }} for Snippet *{{ job.snippet.name }}* (#{{ job.snippet.id }}) was just canceled.",
        "color": "#a41515"
  }]
}
