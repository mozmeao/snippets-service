sudo: false
language: python
git:
  submodules: false
python:
  - "2.7"
before_script:
  - mysql -e 'create database snippets_db;'
install:
  - pip install tox coveralls
env:
  - TOX_ENV=flake8
  - TOX_ENV=docs
  - TOX_ENV=tests
script:
  - tox -e $TOX_ENV
after_success:
  - coveralls
